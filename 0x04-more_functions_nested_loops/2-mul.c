# include "main.h"

/**
 * mul - multiples of intergers
 * @x: The first number to multiply
 * @y: The second number to multiply the first number
 * Return: The results of multiplication between x param and y param
 */

int mul(int x, int y)
{
	return (x * y);
}
